# VTB_backend
皆のVTB backend

##
* install laravel

`composer global require laravel/installer`

* install module

`composer install`

* migrate database

`php artisan migrate`

* generate key

`php artisan key:generate`

* start server

`php artisan serve`
